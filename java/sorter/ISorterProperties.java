public interface ISorterProperties {

    /**
     * This interface is implemeneted by both the Singleton and Proxy examples.
     *   it isn't absolutly needed, but rather a useful tool to show the relationship 
     *   between patterns and how we can use multiple patterns to give ourselves the 
     *   access extensibility we need
     * 
     * @return String
     */
    public String getSorterImplemetion();
    
}